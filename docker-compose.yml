version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webquerytool-api
    ports:
      - "5000:80"
      - "5001:443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
    restart: unless-stopped
